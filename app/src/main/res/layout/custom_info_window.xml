<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@android:color/white">

    <TextView
        android:id="@+id/info_window_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="PACCAR Hall"
        android:textSize="8pt"
        android:textColor="@android:color/black"
        android:layout_marginLeft="@dimen/half_standard_margin"
        android:layout_marginTop="@dimen/half_standard_margin"/>

    <ImageView
        android:id="@+id/info_window_locked_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_lock_black"
        android:layout_alignBottom="@id/info_window_title"
        android:layout_alignParentRight="true"/>

    <TextView
        android:id="@+id/info_window_address"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="address address address"
        android:layout_below="@id/info_window_title"
        android:layout_marginLeft="@dimen/half_standard_margin"
        android:layout_marginBottom="@dimen/half_standard_margin"/>

</RelativeLayout>